<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Fédération National des Mangeurs{% endblock %}</title>
        
        <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    </head>
    <body style="background-color: #1b1b1b; overflow: hidden;">
        
            {% block body %}
                {% set sessionValue = app.session.get('session_key') %}
                <div class="mise_en_page text-3xl" style="background: linear-gradient(to bottom, #F2B416 , #000000 );padding-bottom: 50%;display: flex;justify-content: center;align-items: center;flex-direction: column;">
                    <a class="btn mt-80" href="{{ path('app_login') }}" style="color: rgb(255,255,255);font-family: Inter, sans-serif;">Se connecter</a>
                </div>
            {% endblock %}
        </div>
    </body>
</html>